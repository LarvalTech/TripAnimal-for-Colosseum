{
  "buildCommand": "cd badge_rewards_program && npm install && npm run build",
  "outputDirectory": "badge_rewards_program/build/client",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
